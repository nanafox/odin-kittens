<% content_for :title, "Editing kitten" %>
<h1>Editing kitten</h1>
<%= render "form", kitten: @kitten %>
<br>
<div>
  <%= link_to "Show this kitten", @kitten %>
  |
  <%= link_to "Back to kittens", kittens_path %>
  |
  <%= link_to "Delete this kitten",
  @kitten,
  data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?",
  } %>
</div>
